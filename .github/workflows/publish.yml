name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    name: Release and publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: npm
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # `main` is protected, so the release commit and tag are pushed with a personal access token whose owner is
          # allowed to bypass the branch rule. The default GITHUB_TOKEN cannot push to a protected branch.
          token: ${{ secrets.RELEASE_TOKEN }}
          persist-credentials: true
          # release-it and the conventional-changelog plugin need the tags and the full history.
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: npm
          registry-url: https://registry.npmjs.org
      # Trusted publishing needs npm >= 11.5.1 (bundled with Node.js >= 22.14); fail loudly if the pinned Node.js ever regresses.
      - name: Check npm version
        run: test "$(printf '%s\n11.5.1\n' "$(npm -v)" | sort -V | head -1)" = "11.5.1"
      - run: npm ci
      - name: Configure the git user for the release commit
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
      # release-it runs `npm run check`, bumps the version from the conventional commits, updates CHANGELOG.md,
      # commits, tags, pushes to main and creates the GitHub release. Only then the `after:release` hook publishes
      # to npm through trusted publishing (OIDC), so a rejected push fails the run before anything reaches npm.
      # With nothing releasable since the last tag it exits 0 without doing anything.
      - run: npx release-it --ci
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
